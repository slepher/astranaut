# Tasks

## Implementation

- [x] Build `ExternalUniformMap` from all imported external macro modules.
- [x] Update remote macro matching to use the remote module from the AST.
- [x] Replace import-order external macro function expansion with uniform-map expansion.
- [x] Add an attribute-only expansion path for the external attribute macro phase.
- [x] Discover local macros after external attribute macro expansion.
- [x] Expand local macro source snapshots using only `ExternalUniformMap`.
- [x] Compile and load the local macro module from the locked source snapshot.
- [x] Build `FinalUniformMap` by merging external and local macro maps.
- [x] Run final expansion with `FinalUniformMap`, skipping local macro functions and related functions.

## Tests

- [x] Add a test where a later imported macro generates a call to an earlier imported macro.
- [x] Add a test where an earlier imported macro generates a call to a later imported macro.
- [x] Preserve existing `outer` / `inner` order tests.
- [x] Add a test proving local macro functions are skipped during final expansion.
- [x] Add a test where a local macro expands into an external macro call and the final pass expands it.
- [x] Add tests for nested macros and macros generated by expansion results.
- [x] Add a test where a macro function directly calls another macro function.
- [x] Add a test where `max_depth` limits a generated macro expansion chain.
- [x] Add a test where an external attribute macro generates a local macro discovered by the new flow.
